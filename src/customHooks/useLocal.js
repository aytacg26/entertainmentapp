import React, { useState, useEffect } from 'react';

const useLocal = (name, value) => {
  //arg will be initial argument and it can be string, number, object or array
  //we will need a func, which will set data value
  //for localStorage we have setItem and getItem
};
